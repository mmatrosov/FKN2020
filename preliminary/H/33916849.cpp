#include <bits/stdc++.h>
using namespace std;

#define all(x) (x).begin(), (x).end()

signed main() {
#ifdef LOCAL
    freopen("input.txt", "r", stdin);
#endif

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n = 30, x = 51;
    cout << n << " " << x << '\n';
    cout << 6 << " ";
    for (int i = 1; i < n; ++i) {
        cout << 5 << " ";
    }
}
